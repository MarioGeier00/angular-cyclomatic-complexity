import { SortByComplexityPipe } from './sort-by-complexity.pipe';

describe('SortByComplexityPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByComplexityPipe();
    expect(pipe).toBeTruthy();
  });
});

