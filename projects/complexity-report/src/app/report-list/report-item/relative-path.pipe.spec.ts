import { RelativePathPipe } from './relative-path.pipe';

describe('RelativePathPipe', () => {
  it('create an instance', () => {
    const pipe = new RelativePathPipe();
    expect(pipe).toBeTruthy();
  });
});
